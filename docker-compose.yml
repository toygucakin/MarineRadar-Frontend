services:
  # Bağımsız Frontend Nginx Web Sunucusu
  frontend:
    build: .
    container_name: marineradar-frontend
    restart: always
    ports:
      - "4000:80"
    environment:
      - VITE_API_BASE_URL=http://localhost:3000
